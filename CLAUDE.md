## Git Rules
- Always develop directly on `main`. Do not create feature branches.
- Always `git checkout main` (or `git checkout -B main origin/main`) at the start of a session.
- Commit and push with: `git push origin main`
- Never push to any other branch.
